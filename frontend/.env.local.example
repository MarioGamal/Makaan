# Optional browser configuration for local development.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_MAP_PROVIDER=accessible_local
NEXT_PUBLIC_MAPBOX_TOKEN=

# To exercise the real map locally, use:
# NEXT_PUBLIC_MAP_PROVIDER=mapbox
# NEXT_PUBLIC_MAPBOX_TOKEN=<a public pk.* token restricted to localhost>
